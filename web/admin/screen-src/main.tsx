import React from 'react';
import ReactDOM from 'react-dom/client';
import ScreenApp from './ScreenApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode><ScreenApp /></React.StrictMode>
);
